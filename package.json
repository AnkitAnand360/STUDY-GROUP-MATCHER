{
  "name": "study-group-matcher-root",
  "version": "1.0.0",
  "description": "Root package manager to concurrently execute frontend and backend servers",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npx concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
